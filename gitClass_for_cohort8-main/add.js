// A simple addition exercise.
function add(firstNumber, secondNumber) {
    return firstNumber + secondNumber;
}

const assignmentScore = 18;
const testScore = 72;
const totalScore = add(assignmentScore, testScore);

console.log(`Victor Fabian's total score is ${totalScore}.`);
